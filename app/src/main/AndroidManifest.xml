<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.cuny.light">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.ReActivities.GalleryReActivity"></activity>
        <activity android:name=".Activities.ResetEmailActivity" />
        <activity android:name=".Activities.DeleteAccountActivity" />
        <activity android:name=".Activities.ResetPwdActivity" />
        <activity android:name=".Activities.SavedMemoryActivity" />
        <activity android:name=".Activities.YourMemoryActivity" />
        <activity android:name=".Activities.DescActivity" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Activities.EditActivity" />
        <activity android:name=".Activities.PortalActivity" />
        <activity android:name=".Activities.GalleryActivity" />
        <activity android:name=".Activities.MenuActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>